{
  "name": "homedex-mvp",
  "version": "1.0.0",
  "scripts": {
    "start:api": "json-server --watch data/db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
{
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "start:api": "node server.js"
  }
}

